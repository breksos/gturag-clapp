[package]
name = "clatch-pipe"
version.workspace = true
edition.workspace = true
description = "Clatch app control pipe: the reference Rust binding (clatch_init, signals)"
license.workspace = true
repository.workspace = true

[dependencies]
clatch-core = { path = "../clatch-core" }
clatch-ipc = { path = "../clatch-ipc" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
getrandom = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
clatch-testkit = { path = "../clatch-testkit" }
